import {Component} from "@common/types";
import {withLazyNess} from "@common/layout";

const AuthModule: Component = () => {

    return (
        <div>Auth</div>
    )
}

export default withLazyNess(AuthModule)